using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Manager : MonoBehaviour
{
    // Start is called before the first frame
    // 

    public float eKillCount,count;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (eKillCount==count)
        {
            //enable boss
        }

        //after change scean
    }
}
